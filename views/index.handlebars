<script type="text/javascript">

      // Load the Visualization API and the corechart package.
      google.charts.load('current', {'packages':['corechart']});

      // Set a callback to run when the Google Visualization API is loaded.
      google.charts.setOnLoadCallback(drawChart);

      // Callback that creates and populates a data table,
      // instantiates the pie chart, passes in the data and
      // draws it.
      function drawChart() {

        // Create the data table.
        var data = google.visualization.arrayToDataTable([
          ['Name', 'Times at Gym', { role: 'style' }],
          ['Hannah', 1, '#C5D2EF'],            // RGB value
          ['Tom', 4, 'lightseagreen'],            // English color name
          ['Andrew', 2, 'gold'],
          ['Matt', 3, 'color: #e5e4e2' ], // CSS-style declaration
        ]);

        var options = {
          'title':'Gym Count',
          'width':700,
          'height':500,
          animation:{
            startup: true,   
            duration: 2000,
            easing: 'out',
            }
        };  

        // Instantiate and draw our chart, passing in some options.
        var chart = new google.visualization.BarChart(document.getElementById('chart_div'));
        chart.draw(data, options);
      }
    </script>
  </head>
  <body>
    <div class="header">
      <div id="mobile-hamburger-closed" class="frow height-100">
        <div class="col-xs-1-4">
          <span class="color-block-landing"></span>
          <div class="frow">
            <div class="justify-start height-100 logo">FitBOARD</div>
          </div>
          
         
        </div>
        <div class="nav col-xs-3-4 height-100">
          <div
            class="frow justify-end items-center height-100 visible-sm visible-md visible-lg"
          >
            <a class="ml-25">
              About
            </a>
            <a class="ml-25">
              Profile
            </a>
            <a class="ml-25">
              Login
            </a>
          </div>
          <div class="frow justify-end height-100 visible-xs">
            <button
              id="mobile-hamburger-menu"
              type="button"
              onclick="hamburgerDisplayLink()"
            >
              <div></div>
              <div></div>
              <div></div>
            </button>
          </div>
        </div>
      </div>
      <div
        id="mobile-hamburger-displayed"
        class="frow row-between items-center small-nav height-100"
        hidden
      >
        <a>About</a>
        <a>Profile</a>
        <a>Login</a>
        <button
          id="collaps-hamburger-menu"
          type="button"
          onclick="hamburgerHideLink()"
        >
          <div></div>
          <div></div>
          <div></div>
        </button>
      </div>
    </div>
    <main class="frow-container mt-20">
      <!-- can be kept as divs or h's pending on what we want to do, just messing with different styles -->
      <h3 class="frow">
        <div class="slogan-font">Compete with your friends on FitBOARD</div>
      </h3>
      <h4 class="frow mt-15">
        Set a challenge, join a group, and start competing. 
      </h4>
      <!-- see above message for details  -->
      <div class="frow mt-30">

          <div class="frow-container shadow-dark mt-30">
      <form novalidate="novalidate" id="userForm">
        <div class="frow gutters">
          <div class="col-md-1-2">
            <input
              type="text"
              maxlength="100"
              required="required"
              name="firstName"
            />
            <label>First Name</label>
            <div id="error-first-name" class="error" hidden>
              This field is required
            </div>
          </div>
          <div class="col-md-1-2">
            <input
              type="text"
              maxlength="100"
              required="required"
              name="lastName"
            />
            <label>Last Name</label>
            <div id="error--name" class="error" hidden>
              This field is required
            </div>
          </div>
        </div>
         <div class="col-md-1-2">
            <input
              type="text"
              maxlength="100"
              required="required"
              name="userName"
            />
            <label>User Name</label>
            <div id="error-user-name" class="error" hidden>
              This field is required
            </div>
        </div>
        <div class="frow gutters">
          <div class="col-md-1-2">
            <input
              type="text"
              maxlength="100"
              required="required"
              name="email"
            />
            <label>Email</label>
            <div class="error" hidden>
              This field is required
            </div>
          </div>
          <div class="col-md-1-2">
            <input
              type="password"
              maxlength="100"
              required="required"
              name="password"
            />
            <label>Password</label>
            <div class="error" hidden>
              <p>
                Log in error:
              </p>
              <p>
                Needs 8 characters
              </p>
              <p>
                Must contain: one letter and one number
              </p>
            </div>
          </div>
        </div>
        <button id="register" class="col-md-1-6 col-xs-1-5 mx-5">Sign Up</button>
        <div class="frow">
        </div>
      </form>
    </div>
        
      </div>
      <div class="frow mt-40">
        <div class="frow mt-30"></div>
        <div id="chart_div"></div>
      </div>
      <div class="frow mt-30">
        <div class="col-md-1-3 frow">
            Show your workouts!
          <div class="small-wrapper workout"></div>
          <!-- <i class="fas fa-dumbbell fa-3x"></i> -->
        </div>
        <div class="col-md-1-3 frow">
          Hang with your friends
          <div class="small-wrapper friends"></div>
        </div>
        <div class="col-md-1-3 frow">
          Win the competition
          <div class="small-wrapper competition"></div>
        </div>
      </div>
    </main>